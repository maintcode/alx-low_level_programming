#include "main.h"

/**
 * main - tests a function that prints if a number is positive, neg\
ative, or zero
 * Return: 0
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
